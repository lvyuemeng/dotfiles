global:
  proxy:
    geph_proxy: "127.0.0.1:9910"
    clash_proxy: "127.0.0.1:9911"
    dev_proxy: "127.0.0.1:31181"

  # data_path: the data path of user project
  # User should define its own path
  data_path:
    LibDb:
      windows: "H:/Storage/LibDb"
    Tableau:
      windows: "H:/Storage/Depiction/Tableau"
    Draw:
      windows: "H:/Storage/Depiction/Draw"